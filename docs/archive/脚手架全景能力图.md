# Gaia 全景能力图

## 1. 一句话定义

Gaia 是 AI 时代的通用应用脚手架：业务构建者可以从场景模板验证简单 Demo，开发者通过
**Developer SDK** 完成接入和扩展，**Execution Runtime** 保证任务按被允许的方式运行，再由
Adapter 接入模型、Context、工具、身份和外部系统。

## 2. 两类用户入口

| 用户 | 首要问题 | 入口 | 产出 |
| --- | --- | --- | --- |
| 业务构建者 | 这个场景能做什么、值不值得继续 | Gaia 文档业务路径、Quick Start | 场景模板、样本、Demo 起点和待补资源 |
| 开发者 | 框架如何运行、怎样接入和扩展 | Gaia 文档开发路径、CLI、Python API | 可运行应用、真实 Adapter、测试和部署 |

两类用户共享同一个生成应用和 Runtime。业务构建者不操作底层组件；开发者不替业务方决定场景
价值、样本代表性和验收标准。

## 3. 全景分层

```mermaid
flowchart TB
    BUSINESS["业务构建者<br/>场景·样本·判断标准"]
    DEVELOPER["开发者<br/>接入·扩展·测试·部署"]
    ENTRY["Gaia Docs & Quick Start<br/>双入口·场景模板·项目初始化"]
    PROJECT["客户私有项目<br/>场景·规则·Policy·数据·Adapter·UI"]
    SHOWCASE["未来 Showcase<br/>只消费稳定公共能力"]

    BUSINESS --> ENTRY
    ENTRY --> PROJECT
    DEVELOPER --> SDK
    DEVELOPER --> PROJECT
    PROJECT --> SDK
    SHOWCASE -. "脚手架稳定后单向依赖" .-> SDK

    subgraph CORE["Gaia Core"]
        direction TB

        subgraph SDK["Developer SDK · 开发框架"]
            direction LR
            SESSION["RunSession<br/>运行上下文"]
            MODEL["ModelGateway<br/>Provider·能力探测·结构化输出"]
            CONTEXT["ContextProvider<br/>证据·事实·来源·缺口"]
            TOOL["Tool SDK<br/>Schema·风险·副作用声明"]
            WORKFLOW["Workflow SDK<br/>start·suspend·resume·inspect"]
            BRIDGE["Agent Bridge<br/>入口与任务转换"]
            EVENT["Event SDK<br/>统一运行事件"]
            CACHE["Cache & Rate Limit SDK<br/>TTL缓存·额度决策"]
        end

        subgraph RUNTIME["Execution Runtime · 运行框架"]
            direction LR
            ENGINE["Run Engine<br/>生命周期·调度"]
            POLICY["Policy Engine<br/>权限·预算·工具·环境策略"]
            SAFETY["Safety Boundary<br/>服务端环境·角色·风险·写入上限"]
            STATE["State & Checkpoint<br/>持久化·暂停·恢复"]
            HUMAN["Human Gate<br/>审批·拒绝·过期·接管"]
            EFFECT["Side-effect Boundary<br/>幂等·台账·补偿"]
            RESILIENCE["Resilience<br/>超时·重试·熔断·降级"]
            VERSION["Version Pinning<br/>模型·Prompt·规则·工具·Policy"]
            AUDIT["Audit Evidence<br/>责任主体·来源·结果·失败类型"]
        end

        SDK --> RUNTIME
    end

    subgraph PROVIDERS["能力实现、Integrations 与客户 Adapters"]
        direction LR
        MP["Model Providers<br/>云端·客户平台·本地 Endpoint·Mock"]
        CP["Context Scaffold<br/>解析·检索·结构化查询·组装"]
        WP["Workflow Engine<br/>LangGraph 或客户现有引擎"]
        TP["Tool & System Adapters<br/>只读查询·受控写入"]
        AP["Agent Platform Adapters<br/>成熟工作台·Lite 入口"]
        IP["Identity & Secret Adapters<br/>身份·角色·凭据"]
        SP["State & Event Stores<br/>状态·Checkpoint·事件·幂等记录"]
        RP["Redis Integration<br/>redis-py Client·Cache·RateLimiter"]
        OP["PostgreSQL Outbox<br/>事务事件·租约·重试·死信"]
    end

    RUNTIME --> PROVIDERS

    subgraph DELIVERY["开发测试与诊断工具"]
        direction LR
        MODES["Runtime Environments<br/>Mock·Sandbox·Customer"]
        TESTKIT["Gaia Test Kit<br/>Dataset·Evaluator·Gate·Replay"]
        OBS["Diagnostics<br/>运行日志·Trace·诊断包·验收报告"]
        ACTUATOR["Actuator & Dev Console<br/>只读配置·组件·健康·运行摘要"]
        SECURITY["Security Defaults<br/>脱敏·密钥·数据隔离·写操作默认关闭"]
    end

    DELIVERY <--> CORE
    DELIVERY <--> PROVIDERS

    subgraph ROADMAP["独立路线图，不进入当前核心"]
        SERVING["P2 · model-serving-kit<br/>GPU·量化·本地推理·灰度·回滚"]
        FACTORY["P3 · model-factory<br/>数据治理·SFT/LoRA·评估·制品发布"]
        MQ["触发后实现 MQ Adapter<br/>Kafka 事件流 / RabbitMQ 工作队列"]
        FACTORY --> SERVING
        SERVING -. "输出标准 Model Endpoint" .-> MP
        OP -. "统一 EventPublisher" .-> MQ
    end
```

## 4. 两个核心框架的分工

| 问题 | Developer SDK | Execution Runtime |
| --- | --- | --- |
| 核心目标 | 让开发更快、更一致 | 让执行可控、可恢复、可追溯 |
| 面向谁 | AI 应用开发者 | 每一次真实 Run |
| 提供什么 | 类型、接口、客户端、声明式契约 | 生命周期、策略、状态、人工、副作用与审计 |
| 它不做什么 | 不做最终权限和高风险决策 | 不定义客户规则，不替代外部能力 |
| 两者关系 | 只通过 Runtime 执行有风险的调用 | 统一执行 SDK 声明的能力与围栏 |

## 5. 一次 Run 如何通过整套脚手架

```mermaid
sequenceDiagram
    participant Entry as "客户入口 / Agent Bridge"
    participant SDK as "Developer SDK"
    participant RT as "Execution Runtime"
    participant Ctx as "ContextProvider"
    participant Model as "ModelGateway"
    participant Tool as "Tool Adapter"
    participant Human as "Human Gate"
    participant Evidence as "Event & State Store"

    Entry->>SDK: RunRequest
    SDK->>RT: create RunSession
    RT->>RT: 服务端环境·身份·Policy·版本·预算预检
    RT->>Ctx: 请求证据与结构化事实
    Ctx-->>RT: ContextEnvelope + gaps
    RT->>Model: 在预算内调用候选 Endpoint
    Model-->>RT: 结构化结果 + 调用元数据
    RT->>RT: Schema·规则·工具定义·权限·风险·环境校验
    alt "需要人工决策"
        RT->>Human: suspend + HumanGate
        Human-->>RT: approve / reject / expire
    end
    RT->>Tool: 幂等只读或受控写入
    Tool-->>RT: result / failure / current state
    RT->>Evidence: Checkpoint·Event·SideEffect·Audit
    RT-->>SDK: RunResult
    SDK-->>Entry: 业务结果或明确失败
```

## 6. P0 必须具备的能力

### 开发框架

- RunSession 和公共契约；
- ModelGateway + OpenAI-compatible Provider + Deterministic Mock；
- ContextProvider、Tool、Workflow、Agent Bridge、Event、Cache 和 RateLimiter 接口；
- 上层对 Provider、Workflow Engine 和客户 Adapter 实现无感。

### 运行框架

- Run 状态机与 Checkpoint；
- 不可绕过的 Policy 和权限检查；
- Human Gate 暂停与恢复；
- 幂等 SideEffectCommand；
- 超时、有限重试、熔断和降级；
- 版本固定、运行事件和审计证据。

### 开发与验证

- Mock / Sandbox / Customer 三档及环境绑定；Sandbox 是集成隔离，不是代码执行容器；
- `controlled-task` 合成测试流；
- 版本化 Dataset、确定性断言、可插拔 Evaluator/Gate 和批量回放；
- 健康检查、日志、Trace、诊断包和验收报告；
- Actuator 与可选 Dev Console 只读投影真实装配和运行状态。

## 7. 明确不进入 P0

- 任何具体行业、部门、客户或流程语义；
- 生产级多 Agent 平台与通信总线；
- 无治理、无 namespace 和权限边界的“通用 Agent Memory”；
- 复杂模型调度与 GPU 运维；
- 本地模型部署套件；
- SFT、LoRA、数据标注和模型工厂；
- 拖拽式平台、插件市场和多租户后台；
- 对 Showcase 的新功能或结构改造。
- 任意代码、Shell 或浏览器执行 Sandbox。
- 未出现多 Worker 或跨系统消费前的 Kafka/RabbitMQ Adapter。

## 8. 实现顺序

1. 固定公共契约和 `controlled-task` 合成测试流；
2. 实现 Run Engine、State、Policy、Human Gate 和 Side-effect Boundary；
3. 实现 ModelGateway、ContextProvider、Tool 和 Workflow 的最小调用链；
4. 用 Deterministic Mock 跑通全部正常与失败样本；
5. 接入一个真实 OpenAI-compatible Model Endpoint；
6. 补齐 Test Kit、Actuator、Dev Console 和诊断能力；
7. 增加 Redis Cache/RateLimiter 与 PostgreSQL Outbox；
8. 出现多 Worker 或跨系统消费后，再选择 Kafka 或 RabbitMQ Adapter；
9. 第二个真实场景出现后，再扩大其他 SDK 表面。

该顺序先保证运行围栏与完整闭环，再增加 Provider 和现场可用性，不从平台功能清单倒推实现。
