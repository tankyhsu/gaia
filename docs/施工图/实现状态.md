# Gaia Framework M6 实现状态

> **历史里程碑快照。** 本文记录 Temporal 迁移之前的 M6/SQL Runtime 实现状态，
> 其中 `PersistentRuntimeEngine`、`startup_recover`、Lease 和 SQL Run/Event 的现状描述
> 已失效。当前执行架构与验收状态以
> [17-TemporalIO-迁移任务清单](17-TemporalIO-迁移任务清单.md)为准。

更新日期：2026-07-27

## 当前阶段

状态：**M6 业务构建者体验核心能力完成首轮实现；`docs/施工图/13-重构施工图-装配打通与Runtime拆解.md`
工程 A（声明式装配打通）全部 7 个任务完成，达成该施工图的**里程碑 M2**；工程 B（状态机
显式化与引擎拆解）B1-B6 全部完成，达成**里程碑 M3**（B1：显式状态迁移表）与**里程碑 M4**
（B5：收尾度量与文档）。工程 F（评审整改，见 §8b）F1-F6 全部完成，含 A2.1 的 AST
导入期纯净性检查——**A2.1 现已实现，不再是遗留缺口**。**

Gaia 已从特定交付 Demo 重构为通用 AI 应用开发与运行框架。核心包不包含客户、行业或
`controlled-task` 业务实现；参考业务仅位于 `examples/controlled_task/`。

施工图 13 的 A–F 六个工程（含评审整改工程 F，见 §8b）现已全部完成，其中 A-E 提交范围
`a20e1c3..8b06f08`（20 次提交）：

| 工程 | 内容 | 关键提交 |
| --- | --- | --- |
| A | 声明式装配打通（M1、M2）**含 A2.1**（导入期纯净性 AST 检查，由工程 F 的 F4 补齐） | `a20e1c3`–`fdc1703`（含 A4b `9773955`）；A2.1 见工程 F |
| B | 状态机显式化与引擎拆解（M3、M4） | `e98dee9`–`1e6a78a`（含 B6 `1e6a78a`） |
| C | 版本指纹与策略收紧覆盖（M5） | `1696554`、`5c8425a` |
| D | 恢复租约与部署边界（M6） | `f85f116`、`6d2b2e1` |
| E | 类型化组件访问、框架版本、认证 SPI | `8b06f08` |
| F | 外部评审阻断项整改（认证身份、OIDC、发布闸门、A2.1、迁移断言、规模表述） | 见 §8b 与下方"工程 F"小节 |

**不再有"仍未实现"的遗留项**：A2.1（导入期纯净性的 AST 静态检查）此前只落地了契约文档，
AST 检查器本身未实现；工程 F 的 **F4 任务已经把它补齐**——`src/gaia/diagnostics/import_purity.py`
的 `scan_module_purity`/`PurityFinding`，接在 `gaia check` 里、在 `configure()` 之前对
`scenarios.modules` 声明的每个模块跑一遍。它是 best-effort 的静态 lint（按解析出的 import
来源匹配一份固定白名单，解析不出来源的调用一律不报），不提供隔离或安全边界，也没有做任何
运行时 monkeypatch（该方案已被明确否决，见 A2.1 卡与 `import_purity.py` 模块 docstring）。
下文"工程 A"小节与"工程 F"小节的措辞已同步更新，不应再出现"A2.1 未实现"与"A-E 全部完成"
并置的表述。

## 已实现能力

- `GaiaApplication`、不可变 `ApplicationContext`、ASGI/AsyncExitStack 资源作用域和失败回滚；
- 严格 `gaia.yaml`、Profile、`--profile`/环境/CLI 覆盖、SecretRef 和配置来源；
- 内置与 import Starter、条件装配、组件依赖图和装配报告；
- Model、Workflow、Context、Tool、Policy 组件契约及 Replay 兼容 Starter；
- 开发期 Gaia Test Kit：版本化 JSON/YAML Dataset、CaseExecutor、Evaluator、QualityGate 和 TestReport；
- 通用 PassRateGate：Case 阈值、Suite 通过率、重复执行聚合、关键 Case 和标签分组门禁；
- 通用 Runtime 依赖注入、HumanGate、副作用 CAS、恢复、审计和 SSE；
- Actuator info/health/components/config/conditions；
- `gaia init/check/doctor/add-workflow/dev/test/starters/migrate` CLI 与可独立安装 wheel；
- 无业务常量、独立部署的 Gaia Dev Console；
- `controlled_task` 参考应用、10 个验收样本和重启持久化验证。
- PostgreSQL 事务持久化、托管连接池和框架内置 `gaia migrate`；
- LangGraph PostgreSQL checkpoint、PostgreSQL long-term memory 与 pgvector；
- 通用 EmbeddingProvider、OpenAI-compatible 实现和硅基流动 reference profile；
- SQLite 默认 profile 和 PostgreSQL 扩展 profile 双路径。
- Runtime Safety Boundary：服务端环境、环境写入上限、工具白名单/角色/风险和 Adapter 环境绑定；
- `sandbox` reference profile，以及生成项目的 mock/sandbox/customer 安全默认配置。
- redis-py Integration、共享 Client、CacheProvider/RateLimiter 与可选开发基础设施；
- Capability Pack 形式的 PostgreSQL Transactional Outbox、进程内 Publisher、租约、重试与 dead letter；
- Core、Integration、Capability Pack 与客户 Adapter 的架构测试边界。
- 稳定机器错误码、人类可读错误、重试语义、处理建议和 Trace ID；
- `gaia doctor` 对数据库、Redis、模型和 Embedding Endpoint 的显式连接诊断；
- Run、HumanGate、错误、Outbox、连接池和 PostgreSQL 等待的通用运行摘要；
- Dev Console 首次进入显示可跳过的 Quick Start；初始化中的项目可选择场景模板和按需组件，
  完成后入口只保留说明，概览只保留当前应用状态；
- 独立 Gaia 文档站以受众分流为首页：业务构建者从能力、模板和 Demo 验证进入，开发者从内部
  机制、项目初始化、CLI 和 API Reference 进入；Console 只保留导航入口，HTTP API 由 FastAPI
  OpenAPI 独立生成；
- Console 配置只读展示生效值与字段来源；Run 详情以结果、模型调用、工具调用、安全决策和事件链五个
  Tab 内聚呈现，审批和测试结果使用指标与表格，不直接展示 JSON；
- Console 近 24 小时运维概览，以及停滞 Run、待确认和基础设施积压视图；
- 应用自有 Workflow 节点与路由生成模板，框架不保存业务 State、节点或规则。
- `ScenarioSpec`、`@scenario` 和 `FunctionScenarioRunner`，普通异步函数仍进入统一受控 Runtime；
- `@read_tool` 与受约束的 `@write_tool`，写入必须声明风险、角色、环境和 `reconcile`；
- `ToolRegistry` 统一 Read/Write Tool；`ApiDependencies.from_scenarios(tools=...)` 与
  `GaiaAppBuilder` 显式装配场景、工具、模型、Prompt 和 Guardrail，不使用全局注册表；
- `ScenarioContext.tools/model/retriever`、Scoped Tool Executor、检索身份绑定、角色/环境/超时/
  Guardrail 检查和无正文
  `ToolInvocation` 证据；
- 等待态 `pending_result`、脱敏 `approval_view` 和 Dev Console 一致展示；
- 版本化 `ActionPlan` 支持严格顺序的多动作、逐动作审批、失败停止、部分结果和重启恢复；
- `ScenarioTestHarness` 与 TypeScript `GaiaClient` 提供无 HTTP 场景测试和统一前端 Runtime Client；
- Quick Start 和 Dev Doc 展示独立 HR Showcase 的三个组合案例；日常 Dev Console 不承载
  Showcase 业务内容；
- `gaia init` 生成基础请求、知识检索或受控操作 Scenario、真实 Runtime 测试和所需组件配置；
- `gaia init` 生成 `create_application()` 工厂，测试不复用已停止的 Application 实例；
- `gaia dev --app ... --reload` 支持完整应用热重载；
- 配置路径统一为 `--config`、`GAIA_CONFIG_PATH`、默认 `gaia.yaml`；Prompt/RAG 资产目录以配置
  文件目录为基准，不再依赖进程 CWD；
- `PromptRef`、`PromptArtifact`、`PromptProvider` 和零配置外部 I/O 的 `prompt-file` Starter；
- `ModelInvocation` 与 `ModelUsage` 工程证据契约，不采集模型私有思维链；
- ADR 固定唯一 Runtime SPI、Prompt Artifact、部署单元和模型观测边界。
- PostgreSQL Prompt Registry：不可变版本、质量证据、mock/sandbox/customer 发布指针、回滚、
  退役约束与审计记录；
- 新 Run 在幂等记录落库前解析 Prompt 指针，并把精确版本和内容哈希固定到 VersionBundle；
- `gaia prompt import/diff/validate/publish/rollback`，发布报告必须绑定同一 Prompt Artifact；
- Dev Console Prompt 菜单稳定显示：disabled 提供 Provider 选择状态，file 提供无正文的
  Artifact 只读清单，postgres 提供版本、环境指针和导入三个 Tab；写路由仅由
  `GAIA_DEVTOOLS_ENABLED=true` 显式开启，Actuator 返回工作区开关状态。
- `InstrumentedModelProvider` 为 Mock 与 OpenAI-compatible Provider 生成同一
  `ModelInvocation` 证据，记录 Token、耗时、重试、错误和可选成本；
- 模型观测只持久化内容哈希与版本关联，不保存 Prompt、响应正文、Secret 或模型私有思维链；
- `SqlAlchemyModelInvocationStore`、`0008_model_invocations` 迁移和按 Run 查询 API；
- 可选 OpenTelemetry Sink；未安装 `otel` extra 时 Core 无强制依赖；
- Dev Console Run Inspector 展示调用数、Token、P95、重试及逐调用摘要，并展示五阶段
  Guardrail 的放行、改写、阻断、异常、规则版本和原因码；
- `DocumentLoader`、`DocumentParser`、`Chunker`、`Retriever`、`Citation` 公共契约；
- 受根目录限制的本地 Loader、UTF-8 文本 Parser 和确定性 FixedWindow Chunker 参考实现；
- `MemoryRagRepository` 使用 active generation 隔离更新中间态，并可在重试时清理旧 Chunk；
- tenant/corpus namespace、角色/用户权限过滤、文档版本、来源 URI、Chunk 哈希和权限依据；
- `rag-postgres` 显式 Starter，组合 PostgresMemoryStore、pgvector 与 Embedding；
- `minimal-cited-rag` Golden Dataset 覆盖命中、角色拒绝和跨知识域检索。
- OpenAI-compatible `generate_stream()`、厂商无关 `ModelStreamChunk` 和流式调用观测；
- 五阶段 Guardrail Pipeline、稳定阻断错误、Pattern 改写；无输出规则时保持原生流式，有输出
  规则时完整缓冲并单次检查；
- `GuardrailDecision` 无正文证据、`0009_guardrail_decisions` 迁移、按 Run 查询和
  fail-open/fail-closed/强审计语义；
- `GaiaAppBuilder` 和 `ScenarioTestHarness` 统一装配五阶段防护，旧
  `tool_guardrails` 仅保留兼容；
- Presidio PII 与 Guardrails AI validate-only 可选适配器，不进入默认依赖；HR Showcase
  已用真实 Guardrails AI Validator 验证结构化模型输出；
- Runtime 原生多 Agent Handoff、共享状态、持久交接证据、启动恢复和 `max_handoffs` /
  RunBudget 双重约束；
- 审批或 ActionPlan 后的持久化 Continuation，可根据 ToolResult 动态进入模型、Handoff 或下一
  次受控写操作；
- Write Tool 的可对账、目标端幂等和最多一次人工处理三种恢复策略；
- `0011_run_budgets`、`0012_runtime_handoff` 与 `0013_runtime_continuation` 迁移；
- TypeScript Client 指南、Java 17 Spring `RestClient` 参考源码和 CI 编译门禁；
- Schema / 显式可纠偏 Guardrail 的 bounded correction，耗尽返回
  `MODEL_OUTPUT_INVALID`；
- `gaia test` 直接运行应用 Test Kit 工厂和 Dataset，输出报告及 `0/1/2` 稳定退出码；
- Dev Console Nginx 动态解析 Compose 服务地址，并能正确处理非 JSON 网关错误。
- `scenarios.modules` 配置键与 `gaia.starters.scenario_discovery.discover_scenarios`：
  按声明的模块路径导入并清点 `@scenario`/`@read_tool`/`@write_tool`，重复 ID 或未知
  Handoff 目标在装配期即报错（`SCENARIO_MODULE_NOT_FOUND`/`_IMPORT_FAILED`、
  `HANDOFF_TARGET_NOT_FOUND` 等），不留到运行期；
- `gaia.runtime.assembly.RuntimeAssembler`：从 `ApiDependencies.from_scenarios` 抽取的
  单一装配路径，供装饰器 API 和 `scenario-runtime` Starter 共用，无行为变化；
- 内置 `scenario-runtime` Starter：`scenarios.modules` 非空时把 `RuntimeAssembler` 注册为
  `runtime-assembler` 组件；`create_app` 在未显式传 `dependencies` 时从组件图取用，模型、
  Prompt、检索三种可选协作者只在组件图里确有满足对应端口的真实实例时才注入；
- `model-mock` / `model-openai-compatible` 现在注册真实 `ModelProvider` 实例（不再是占位
  marker dict），declarative 场景可直接用 `ctx.model` 调用模型，无需应用提供
  `model_provider`；WORKFLOW/CONTEXT/POLICY 仍是占位 marker dict；
- `@agent_handler(agent_id, allowed_handoffs=...)` 与 `@continuation_handler(name)`：
  与 `@scenario` 一样只贴不可变元数据，由 `discover_scenarios` 发现并校验，路由表没有
  隐式默认边；
- `examples/function_task`：纯声明式参考应用，`app.py` 不到 15 行且无手工装配代码，覆盖
  只读读取、写入审批（HumanGate）、Agent Handoff 与写后 Continuation 四条路径；
- `gaia init` 生成的项目改为声明式装配：`gaia.yaml` 声明 `scenarios.modules` 并包含
  `scenario-runtime` Starter，生成的 `app.py` 只调用
  `create_app(gaia_application=GaiaApplication.from_config(resolve_config_path()))`，
  不再生成 `GaiaAppBuilder` composition 代码；`basic`/`knowledge`/`approval` 三个模板
  均适用，Prompt Provider（`prompt-file`/`prompt-postgres`）与 Retriever
  （`rag-postgres`）均通过组件图自动注入。

Test Kit 提供测试骨架、确定性默认 Gate 和显式启用的通用通过率 Gate，不内建置信区间、
LLM judge 或评估平台。应用可以通过公共协议注入自己的指标与质量决策模型。

## 任务状态

| 任务 | 状态 | 主要提交 |
| --- | --- | --- |
| T00 设计冻结与基线 | 完成 | `59c78fe`、`09b129d` |
| T01 配置契约 | 完成 | `294c49a` |
| T02 Component/Starter/AutoConfig | 完成 | `294c49a`、`88f0191` |
| T03 GaiaApplication/Actuator Snapshot | 完成 | `294c49a`、`88f0191` |
| T04 Runtime 解耦与示例化 | 完成 | `694d05e` |
| T05 Actuator/运行投影 | 完成 | `91d0429`、`694d05e` |
| T06 CLI 与生成项目 | 完成 | `43b5e45`、`9af11ec` |
| T07 Dev Console | 完成 | `f4ca3cc`、`acf7cbc` |
| T08 全量质量 | 完成 | 见下方证据 |
| T09 迁移与文档清理 | 完成 | 本状态记录对应提交 |
| T10 独立验证 | 完成 | clean-wheel 与公开 HTTP 黑盒验证 |
| T11 PostgreSQL 与 Agent 持久化 | 完成 | 真实 pgvector/PostgreSQL 集成与 Compose 验证 |
| T12 Runtime 安全边界与 Sandbox | 完成 | 本状态记录对应提交 |
| T13 Redis 与 PostgreSQL Outbox | 完成 | 本状态记录对应提交 |
| T14 Python 原生资源与集成边界 | 完成 | 本状态记录对应提交 |
| T15 通用可运维性与应用扩展模板 | 完成 | 本状态记录对应提交 |
| T16 接入中心与生产部署隔离 | 完成 | 本状态记录对应提交 |
| T17 M0 架构契约收敛 | 完成 | 本状态记录对应提交 |
| T18 M1 场景最短路径 | 完成 | 本状态记录对应提交 |
| T19 M2 Prompt 生命周期 | 完成 | 本状态记录对应提交 |
| T20 M3 模型调用可观测 | 完成 | 本状态记录对应提交 |
| T21 M4 最小有引用 RAG | 完成 | 本状态记录对应提交 |
| T22 M5 评审 P1/P2 增强 | 完成 | 本状态记录对应提交 |
| T23 M5 Guardrail 纵深防护 | 完成 | 本状态记录对应提交 |
| T24 Agent 研发 SOP 与 CI | 完成 | Codex Hook、Change Set、GitHub workflow |
| T25 M6 业务构建者体验 | 完成首版 | Tool Executor、等待态契约、ActionPlan、Harness、Client 与 Showcase |

### 施工图 13：装配打通与 Runtime 拆解 -- 工程 A

`docs/施工图/13-重构施工图-装配打通与Runtime拆解.md` 是独立于上表 T00-T25 的第二条任务编号
（A/B/C/D/E，与 T 系列无关）；下表的"里程碑 M2"是该施工图自己定义的 M1-M6（A4+A4b 为 M1、
A7 为 M2……），不是上表 T19 使用的框架级 M2。

| 任务 | 状态 | 主要提交 |
| --- | --- | --- |
| A1 配置模型：`scenarios` 段 | 完成 | `a20e1c3` |
| A2 模块扫描器 `scenario_discovery` | 完成 | `7d67463` |
| A3 抽取 `RuntimeAssembler`（无行为变化） | 完成 | `3d2b583` |
| A4 `scenario-runtime` Starter + `create_app` 接线 | 完成 | `8ebcd5e` |
| A4b 核心 Starter 注册真实实例 | 完成 | `9773955` |
| A5 声明式参考应用 `examples/function_task` | 完成 | `89bc9d1` |
| A6 handoff / continuation 的声明式发现 | 完成 | `80c9a35` |
| A7 `gaia init` 模板与文档收口 | 完成 | 本状态记录对应提交 |

工程 A（A1-A7，含 A4b）全部完成，达成里程碑 **M1**（A4+A4b：只读与模型两类声明式场景
均无需手工装配）和 **M2**（A7：`gaia init` 生成的项目、`examples/function_task` 与 Dev
Console 引导式初始化全部改为声明式装配，`app.py` 不再出现 `GaiaAppBuilder`）。

曾经明确未完成、现已补齐的部分：

- 施工图 13 §8 终态断言 4 提到的"Scenario 模块顶层做 I/O 时 `gaia check` 返回退出码 2"
  （任务 A2.1，AST 级 import-purity 静态检查）**此前未实现，现已由工程 F 的 F4 任务补齐**：
  `gaia.diagnostics.import_purity.scan_module_purity` 用 AST 解析目标模块源码（不 import
  目标模块本身），按导入来源匹配固定白名单，`gaia check` 在 `configure()` 之前对
  `scenarios.modules` 声明的每个模块跑一遍，命中即退出码 2、findings 写入 `issues`；
  `discover_scenarios` 本身仍然只在真正执行 `import` 失败时报告
  `SCENARIO_MODULE_NOT_FOUND` / `SCENARIO_MODULE_IMPORT_FAILED`，不做 AST 扫描——这是
  设计上的分工，不是遗漏（AST 扫描是 `gaia check` 独立触发的辅助步骤，见
  `tests/unit/test_import_purity.py` 与 `tests/cli/test_main.py`）；
- 工程 C、D、E、F 均已完成（见文件开头的提交范围表与"工程 F"小节）；下列历史条目描述的是
  它们完成前的状态：
- （历史）工程 C（版本指纹与策略收紧覆盖）、工程 D（恢复租约与部署边界）、工程 E（清理项）均未
  开始；工程 B（含 B6）已全部完成。

### 施工图 13：装配打通与 Runtime 拆解 -- 工程 B

| 任务 | 状态 | 主要提交 |
| --- | --- | --- |
| B1 显式状态迁移表 | 完成 | `e98dee9` |
| B2 抽取 `CommandExecutor` | 完成 | `d2ee5ae` |
| B3 抽取 `ActionPlanManager` | 完成 | `21037de` |
| B4 抽取 `HandoffCoordinator` 与恢复逻辑 | 完成 | `b7a7d07` |
| B5 收尾度量与文档 | 完成 | 本状态记录对应提交 |
| B6 合并账本：消除 `RuntimeLedger` 与引擎私有管道的双份实现 | 完成 | 本状态记录对应提交 |

工程 B（B1-B6）全部完成，达成里程碑 **M3**（B1：`RunStatus`/`GateStatus`/`CommandStatus`/
`ActionStatus` 四张迁移表在 `src/gaia/runtime/lifecycle.py` 落地，Runtime 的每一处状态写入
都经过对应的 `validate_*_transition`）和 **M4**（B5：收尾度量与文档）。

`PersistentRuntimeEngine` 的体积轨迹（纯搬移，零行为变化，每一步之后现有测试零断言改动
通过）：

| 阶段 | `persistent_engine.py` 行数 | 变化 |
| --- | --- | --- |
| B2/B3/B4 之前 | 1980 | -- |
| B2 后（抽取 `CommandExecutor`） | -- | -393 |
| B3 后（抽取 `ActionPlanManager`） | -- | -341 |
| B4 后（抽取 `HandoffCoordinator`） | -- | -142 |
| B5 侦察时（当前） | **1146** | 累计 -834 |

三个协作者文件（B5 侦察时点的行数）：`src/gaia/runtime/command_execution.py` 504 行、
`src/gaia/runtime/action_plan.py` 424 行、`src/gaia/runtime/handoff.py` 219 行；`lifecycle.py`
189 行。B5 的断言性检查确认这四个方法组（B2/B3/B4 搬走的全部方法）在
`src/gaia/runtime/` 下都只存在唯一一份定义，没有残留副本——抽取是彻底搬移，不是复制。

B5 卡片原定"降到 800 行以下"的规模目标，已基于 B2-B4 实测数据修正为**≤1200 行**：剩余 30
个方法中，10 个是公开 API、6 个是核心编排，这两组是引擎的本职，不应再拆；另外 12 个是被三
个协作者共用的账本管道方法，真正能再降的规模在这里，但这不是"再切一刀"式的搬移，而是需要
与既有但生产未使用的 `RuntimeLedger` 合并，因此被记录为新的 B6 任务卡，而不是塞进 B5 里
勉强凑数字。完整理由见 `docs/施工图/13-重构施工图-装配打通与Runtime拆解.md` 的 B5 卡"规模
目标修正"小节。

**B6 已完成**：B5 侦察发现 `src/gaia/runtime/ledger.py` 的 `RuntimeLedger`
与引擎私有的事件/快照管道（`_append_event`、`_record_step`、`_transition` 等）功能重叠。判定
去留时读代码而非凭名字：`RuntimeLedger.event()` 用"先 `SELECT max(sequence)` 再 `+1`"分配事件
序号，并发写入下会取到同一个号；引擎的 `_append_event` 用一条原子的
`UPDATE runs SET event_sequence = event_sequence + 1 RETURNING event_sequence` 语句取号，
迁移 `0002_run_event_counter`/`0003_backfill_run_event_counter` 正是为修复这个竞态而引入
`runs.event_sequence` 计数器的。`RuntimeLedger` 生产代码从未引用，仅
`tests/integration/test_persistence.py` 使用，是修复竞态后遗留的旧实现。结论：删除
`RuntimeLedger`，把该测试改为经引擎真实的 `transition`/事件落库路径验证同一性质（Run 与
Event 跨 session 持久化、序号原子分配）。顺带用同样的 grep 检查了
`src/gaia/runtime/recovery.py`（6 行，包装 `engine.startup_recover()` 的
`recover_runtime()` 函数）：生产代码（`src/gaia/api/app.py`）直接调用
`runtime_engine.startup_recover()`，**不经过** `recover_runtime()`；`recover_runtime` 仅被
`tests/integration/test_recovery.py`、`tests/integration/test_runtime_reliability.py` 引用，
是同一种"生产不用、只有测试在用"的模式。本任务范围只覆盖 `RuntimeLedger`，`recovery.py` 按
任务卡要求不在本次处理，留给后续任务显式决策。

### 施工图 13：装配打通与 Runtime 拆解 -- 工程 F（评审整改）

外部评审判定"不能按重构完成验收"，列出 4 项阻断 + 1 项 P2（施工图 13 §8b）。工程 F 是
整改任务卡，**全部完成前施工图 13 不得标记为完成**——现已全部完成。

| 任务 | 内容 | 状态 |
| --- | --- | --- |
| F1 | 认证身份必须贯穿所有接口，审批人由服务端生成，不信任请求体 | 完成 |
| F2 | 内置 OIDC/JWT `AuthnProvider`（企业 IAM 对接） | 完成 |
| F3 | `make verify` 发布闸门修复：`package_smoke.py` 先安装生成项目再 `gaia check` | 完成 |
| F4 | 实现 A2.1 的 AST 导入期纯净性检查 | 完成 |
| F5 | PostgreSQL 迁移断言改为比对 Alembic 自报的 head，而非硬编码版本号 | 完成 |
| F6 | 规模目标与状态表述：记录 `persistent_engine.py` 实测行数上浮原因，消除本文档"A-E 全部完成"与"A2.1 未实现"的并置矛盾表述 | 完成 |

逐项说明：

- **F1/F2**：见 `CHANGELOG.md` "Unreleased" 中对应条目（`IDENTITY_MISMATCH` 错误码、
  `gaia.integrations.oidc.JwtAuthnProvider`）。
- **F3**：`scripts/package_smoke.py` 原先在生成项目安装（`uv pip install -e`）之前就跑
  `gaia check`；声明式装配落地后 `gaia check` 会 import `scenarios.modules`，未安装的
  生成项目必然报 `SCENARIO_MODULE_NOT_FOUND`。现改为先装后查，与 A7 已经写进 `gaia init`
  输出和生成 README 的"先安装再 check"契约一致。`make package-smoke` 通过即为验收。
- **F4**：见本文件"工程 A"小节的补齐说明与 `src/gaia/diagnostics/import_purity.py` 模块
  docstring；新增 `tests/unit/test_import_purity.py`（别名解析、函数体/局部类不误报等
  用例）与 `tests/cli/test_main.py` 的 `gaia check` 用例。
- **F5**：`tests/integration/test_postgres_stack.py` 原断言
  `alembic_version == "0010_business_builder_runtime"`（迁移头早已推进到
  `0014_runtime_leases`，断言已过期）；改为断言等于
  `gaia.persistence.migrate.current_head(...)`（新增函数，只读 Alembic `ScriptDirectory`
  上报的 head，不连数据库），使断言不再随新迁移过期。已用本地 `docker compose -f
  infra/dev/compose.yaml up -d` 起 PostgreSQL 并执行
  `uv run pytest -q -m "postgres and not external"` 验证通过。
- **F6**：`persistent_engine.py` 实测 **1306** 行，超过 B5 修正后的 ≤1200——C2（版本指纹与
  策略收紧覆盖）新增的租约续租逻辑与 D1（恢复批处理循环）之后又向引擎加了编排代码。**不为
  对齐数字再拆一刀**：B5 卡已经论证过，剩余方法要么是公开 API、要么是核心编排，拆出去只会
  把耦合藏进新文件。B5 卡与施工图 13 §8 终态断言第 5 条已更新为记录实测值 1306 与上浮原因，
  不再是一个会被违反的硬性上限。本节与本文件开头的"当前阶段"、工程 A 小节的措辞已同步
  更新，"A-E 全部完成"与"A2.1 未实现"不会再同时出现在本文档中。

## 验证证据

```text
ruff check: passed
ruff format: 本次修改文件通过；仓库保留 1 个既有开发脚本的格式差异
mypy: 136 source files passed
pytest deterministic suite: 255 passed, 12 service/external tests deselected, 2 third-party warnings
Codex Hook: SessionStart/UserPromptSubmit 注入规约，PreToolUse 阻断未验证提交，Stop/SubagentStop 收口
GitHub CI: Python/契约/文档、前端 E2E、PostgreSQL/Redis、clean-wheel 四个并行 Job
GitHub Nightly/Release: 研发阶段不启用；外部模型仅允许本地显式 opt-in，暂不对外发布制品
P1/P2 focused tests: model stream、五阶段 Guardrail、multi-Agent Handoff、Profile CLI and gaia test passed
Gaia Test Kit: 9 focused tests passed (loader、custom evaluator/gate、PassRateGate 和失败证据)
Runtime Safety Boundary: mode/env、Sandbox approval、Customer deny、role/tool/risk tests passed
web TypeScript/Vite build: passed
Playwright: 6 passed (Quick Start 覆盖三个 Showcase 入口，日常 Console 不展示业务案例)
TypeScript Client: standalone typecheck and OpenAPI drift check passed; HR Showcase consumes createRun/getGate/decideGate
Java Client: OrbStack Maven 3.9 / JDK 17 `clean verify` passed; GitHub CI repeats the compile
Development startup: API、Dev Console、文档使用独立本地命令；单一 Compose 只包含 PostgreSQL/Redis
cleanup migration: 0006 reached; retired application/configuration tables absent
retired configuration API: returns 404; Actuator contains no retired fields
public HTTP clients: read、HumanGate、SSE 和跨审批持续 SSE passed
isolated deployment smoke: Run、events、诊断包和 replay 10/10 passed
external generated project: install/new/add-workflow/pytest/check/doctor passed
M1 clean-wheel project: install/new/pytest/check/prompt resolve/HTTP Run/reload passed
M2 clean-wheel project: wheel install/new/pytest/check/HTTP Run/exact Prompt pin/reload passed
M3 clean-wheel project: default wheel import/new/pytest/check passed without OpenTelemetry installed
Prompt Registry PostgreSQL: migration/import/validate/publish/resolve passed
Prompt CLI PostgreSQL black box: import/validate/publish/diff/rollback passed
Prompt production isolation: default OpenAPI contains no Prompt write routes
Model observability: Mock/OpenAI-compatible success、retry、failure、usage and sink isolation passed
Model evidence privacy: Prompt and response bodies absent from persisted/API/OTel evidence
PostgreSQL model/Guardrail evidence: 0009 migration、full Run、record and Run projection passed
RAG Golden Dataset: finance hit、role denial and HR hit passed with Citation evidence
PostgreSQL RAG: idempotent ingest、replacement、ACL filter、citation and delete passed
M4 clean-wheel project: rag-postgres generation/check/startup passed against OrbStack PostgreSQL
custom imported Starter: defaults/component/Actuator passed
controlled-task: read + write + HumanGate + events passed
controlled-task replay: 10/10 passed
same-database restart: completed Run and events remained queryable
PostgreSQL: transaction/rollback + complete Run passed
PostgreSQL checkpoint: provider restart/resume passed
PostgreSQL memory: CRUD/filter/vector/restart passed
SiliconFlow: 64-dimension embedding + pgvector Chinese retrieval passed
PostgreSQL Compose: migration/health/Actuator redaction passed
Redis: real TTL + Lua atomic rate limit + shared Client resource passed
PostgreSQL Outbox: migration + concurrent SKIP LOCKED + dispatch passed
PostgreSQL observability: pool/active wait/lock wait summary passed
Python lifecycle: configure zero-I/O + dependency injection + AsyncExitStack rollback passed
external integration rerun: PostgreSQL、Redis、SiliconFlow 10 passed
M6 service integration: PostgreSQL/Redis 10 passed against local OrbStack infrastructure
```

测试 Case 审计：

- 10 个 controlled-task acceptance Case 分别覆盖读取成功、输入无效、未知写意图、审批成功、
  越权读取、上下文不足、未知副作用、幂等重复、人工拒绝和重启恢复，没有重复预期路径；
- 本轮审计 69 个 Python 测试文件、256 个测试函数和 267 个收集 Case；删除了只覆盖旧
  Showcase Mock Read Tool 的过期 Contract Test，以及只服务已撤销场景目录 API 的集成测试；
- AST 归一化未发现完全相同的测试函数体；
- PostgreSQL / SQLite 迁移黑盒断言已更新到当前
  `0012_runtime_handoff`，并验证 `run_budgets` 与 `runs.handoff_json`；
- 原 HTTP replay 集成测试再次执行全部 10 Case，与 acceptance 层重复，已收敛为单 Case 选择、
  执行和响应契约验证；完整 10 Case 行为仍由 acceptance 层负责；
- Unit、Contract、Integration、Architecture、Acceptance、CLI 和 E2E 各自验证不同边界，
  未发现可删除而不损失风险覆盖的整组测试。

独立验证曾发现并已修复：

- 生成应用缺少 SQLite 父目录，导致首次启动失败；
- 宽泛 LangGraph 版本范围可解析到不兼容组合；
- 浏览器产物曾硬编码开发 API key。

## 明确边界

- M1 不提供 HTTP 配置写入或运行时热替换，配置变更通过文件、环境和部署流程完成；
- Gaia 不负责容器、Kubernetes 或其他部署编排，部署层应用配置后以 Actuator 快照为准；
- import Starter 只能由代码和 `gaia.yaml` 声明；
- 两个现存 warning 来自 LangGraph checkpoint 与 Starlette TestClient 的上游弃用提示，不影响 M1 功能；
- Showcase 仍保持独立，后续只能消费 Gaia 公共能力。
- Gaia 运行摘要只包含框架级 Run、Gate、错误和基础设施状态；客户业务指标留在应用或外部可观测平台。
- Gaia Dev Console 作为独立本地开发进程显式启动，不进入生产应用安装包或基础设施服务图。
- Sandbox 是测试资源、凭据和 Adapter 的集成隔离；任意代码执行 Sandbox 仍为明确非目标。
